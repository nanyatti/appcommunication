<Window x:Class="app_wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:app_wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525"
        Loaded="Window_Loaded">
    <Grid>
        <TextBlock x:Name="textBlock" Margin="0,0,0,150" FontFamily="Segoe UI" FontSize="46"/>
        <TextBox x:Name="inputTextBox" Margin="0,180,0,90" FontFamily="Segoe UI" FontSize="15"/>
        <TextBlock x:Name="logTextBlock" Margin="0,240,0,30" FontFamily="Segoe UI" FontSize="15"/>
        <Button Content="Send"
                Click="Button_Click"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom" FontFamily="Segoe UI" FontSize="15"/>
    </Grid>
</Window>
